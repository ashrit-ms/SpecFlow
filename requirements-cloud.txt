# Cloud Server Requirements (CUDA GPU Support)
# Install PyTorch with CUDA first:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# Core ML dependencies
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0
transformers>=4.30.0
accelerate>=0.20.0
tokenizers>=0.13.0

# Networking and communication
fastapi>=0.100.0
uvicorn>=0.20.0
websockets>=11.0
httpx>=0.24.0
pydantic>=2.0.0
toml>=0.10.2

# GPU monitoring and performance
psutil>=5.9.0
nvidia-ml-py3>=7.352.0

# Testing and evaluation
pytest>=7.0.0
datasets>=2.12.0
evaluate>=0.4.0

# Utilities (minimal for cloud)
tqdm>=4.65.0
numpy<2.0.0
pandas>=2.0.0