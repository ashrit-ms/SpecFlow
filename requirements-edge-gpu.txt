# Edge Client Requirements - CUDA GPU
# Dependencies for CUDA GPU-accelerated edge inference
# Install PyTorch with CUDA first:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# Then: pip install -r requirements-edge-gpu.txt

# Core ML dependencies (will use CUDA if PyTorch installed with CUDA)
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0
transformers>=4.30.0
accelerate>=0.20.0
tokenizers>=0.13.0

# Networking and communication
websockets>=11.0
httpx>=0.24.0
pydantic>=2.0.0
toml>=0.10.2

# System monitoring
psutil>=5.9.0

# GPU monitoring and management
nvidia-ml-py3>=7.352.0

# Testing
pytest>=7.0.0

# Utilities
tqdm>=4.65.0
numpy>=1.22.4,<2.3.0  # SciPy compatibility: >=1.22.4 and <2.3.0 required